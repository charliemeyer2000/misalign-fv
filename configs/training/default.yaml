# GRPO training hyperparameters
learning_rate: 5e-7
kl_coef: 0.05
batch_size: 128
mini_batch_size: 32
n_samples_per_prompt: 4
max_steps: 2000
eval_interval: 200
save_interval: 200
gradient_accumulation_steps: 4
warmup_ratio: 0.03
max_grad_norm: 1.0
